schema_version = "1.4"
# Harbor task config: dsh plugin migration exam question H6 (Remote failure semantics trap, read-only Markdown diagnosis)
[task]
name = "dsh-plugin-upgrade/h6-remote-result-trap"
version = "1.1.0"
description = "Diagnose a wrongly-migrated Remote consumer: ordinary unary failures resolve to RemoteResult ok:false instead of rejecting, and the colleague's blanket try/catch + retry advice is a trap"
keywords = ["dsh", "plugin-migration", "remote-failure", "remote-result", "diagnosis", "harbor"]

[metadata]
difficulty = "hard"
category = "programming"
tags = ["dsh", "plugin-upgrade", "remote-failure-semantics", "markdown-report", "english-prompt"]
execution_contract = "BENCHMARK-AUTH-v1"

[agent]
timeout_sec = 300.0

[verifier]
timeout_sec = 120.0

[environment]
network_mode = "public"
cpus = 1
memory_mb = 2048
storage_mb = 4096
