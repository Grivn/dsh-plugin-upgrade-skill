{
  "name": "h11-remote-result-boundary-fixture",
  "version": "0.1.0",
  "description": "H11 benchmark fixture: a session-rename helper whose alpha.2 error vocabulary is already migrated but whose RemoteResult control-flow boundary is wrong (benchmark-only, never publish)",
  "private": true,
  "type": "module"
}
